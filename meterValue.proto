syntax = "proto3";

package spectral_meterusage;

// Request for meter usage values
message Date {
    string date_str = 1;
}

// Response for meter usage values
message dateValue {
    string values = 1;
}

// Service Definition
service meterUsageValues {
    rpc getMeterData (Date) returns (dateValue) {}
}